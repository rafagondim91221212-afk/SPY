// This script is referenced by the Vercel project's custom build command:
//   node .v0/inject-built-with-v0.mjs && next build
// It previously did not exist in the repository, which caused the deploy to
// fail with "exited with 1" before `next build` could run.
// It is intentionally a safe no-op so the build command always succeeds.
process.exit(0)
